import variable scouted_crypts;

procedure start;


procedure start
begin
	if (script_action == 2) then begin// spatial_p_proc - Player is on or near hex of the location or object this script is using
		if (scouted_crypts) then begin
			scouted_crypts := 2;
			set_global_var(178, 2);
		end
	end
end

